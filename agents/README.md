# Cortex Agents

Deterministic underwriting pipeline:

```txt
Parser -> FX -> Verification -> Risk -> Attestation
```

Run:

```bash
pnpm --filter @cortex/agents test
pnpm --filter @cortex/agents typecheck
```
